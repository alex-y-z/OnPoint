

// A class to represent each player in the game.
class Player {
    constructor () {

    };
    
    // Array to hold locations(regions) of each dart
    dartsThrown = [0, 0, 0];
    score = 0;

    /*
    Description: Saves the last score to the database,
                 Resets the dartsThrown array, and restores
                 remaining darts.
    Parameters: 
    Returns: 
    */
    beginTurn = () => {

    };

   

    // JS command to send to HTML
    // var playerName = ...
    // var name = document.getElementById('player1');
    // name.innerHTML = playerName;


}

// A class to represent each leg of the match.
class Leg {
    constructor () {

    };

    // Pulls from the database
    

    /*
    Description: Takes the values from the thrownDarts array to update
                 the score for the player.
    Parameters:
    Returns:
    */
    calculateScore = () => {

    };


}

// A class to represent each match.
class Match {
    constructor () {

    };

    





}

// A class to represent the scoreboard.
class Scoreboard {
    constructor () {

    };

    

}